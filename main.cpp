#include <stdio.h>
#include <stdlib.h>
#include <vector>
#include <string>
#include "tools.hpp"
#include "parser.hpp"


int main()
{
    std::vector<std::string> tokens = readTokens();
    //printTokens(tokens);
    //Node* tree = parser(tokens);
    return 0;
}
