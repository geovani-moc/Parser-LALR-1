#ifndef TOOLS_HPP
#define TOOLS_HPP

#include <vector>
#include <stdio.h>
#include <string>
#include <iostream>

using namespace std;

vector<string> readTokens();
void printTokens(vector<string> &tokens);


#endif